flashes_redraw("<%= j render partial: 'layouts/flashes' %>");
$('#template_grid')
  .removeClass('active')
  .addClass('success')
  .prepend("<%= j render partial: 'item', locals: {template: @template } %>");
$('#new_template_modal').modal_success();
