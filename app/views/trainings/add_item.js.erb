flashes_redraw("<%= j render partial: 'layouts/flashes' %>");
$('#trainings_table td.'+<%= @training.from.hour.to_s+@training.date.wday.to_s %>)
  .removeClass('active')
  .addClass('success')
  .prepend("<%= j render partial: 'item', locals: {training: @training } %>");
$('#new_training_modal').modal_success();
