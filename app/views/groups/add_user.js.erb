flashes_redraw("<%= j render partial: 'layouts/flashes' %>");
$('#available_grid #user-<%= @user.id %>').remove();
$('#members_grid').append("<%= j render partial: 'user', locals: {user: @user, group: @group, width: 6 } %>");
